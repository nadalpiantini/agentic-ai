/**
 * Application name identifier
 */
export const APP_NAME = "Agentic Hub" as const;

/**
 * Maximum number of retry attempts for failed operations
 */
export const MAX_RETRIES = 3 as const;

/**
 * Default timeout for asynchronous operations in milliseconds
 */
export const DEFAULT_TIMEOUT_MS = 30000 as const;

/**
 * API version string for endpoint versioning
 */
export const API_VERSION = "v1" as const;
